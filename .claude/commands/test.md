Run the accompy test suite and report results.

```bash
cd /Users/thorwhalen/Dropbox/py/proj/t/accompy && python -m pytest tests/ -v --tb=short
```

After running:
1. Report the pass/fail summary
2. If any tests fail, identify the failing tests and diagnose the cause
3. Suggest fixes for any failures
